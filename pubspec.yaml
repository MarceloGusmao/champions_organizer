name: champions_organizer
description: 'A new Flutter project.'

publish_to: 'none' # Remove this line if you deseja publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.2.1
  cupertino_icons: ^1.0.6
  parse_server_sdk_flutter: ^8.0.0
  google_fonts: ^4.0.0
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
